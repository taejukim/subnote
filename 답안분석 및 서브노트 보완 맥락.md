# 보안 서브노트 보완 — 캡처 PNG 기준 평가·작성 가이드

> **대상:** ITPE 정보관리기술사 보안(6번) 도메인  
> **목적:** NS반 강의자료 **캡처 PNG**를 기준(Source of Truth)으로, 기존 서브노트 대비 **누락 토픽을 평가**하고 **동일 포맷으로 보완**하는 절차를 다른 사람도 그대로 재현할 수 있도록 정리  
> **작업 기준일:** 2026-07-21

---

## 1. "캡처된 답안"이란?

NS반 강의 PDF(또는 슬라이드)를 **페이지별 PNG로 캡처**한 파일이다. 각 PNG 1장 = 토픽 1개의 **모범 답안·정리본** 역할을 한다.

| 구분 | 경로 | 역할 |
|---|---|---|
| **캡처 PNG (기준)** | `1. NS반/서브노트/보안/*.png` | 평가·작성의 **유일한 기준** — "이 토픽은 강의에서 이렇게 정리됨" |
| **토픽 목차 (보조)** | `1. NS반/서브노트/보안.md` | 캡처 내용을 숫자·위키링크로 정리한 **체크리스트** (85토픽) |
| **서브노트 (대상)** | `0. Sub-Note/6. 보안/*.md` | Obsidian 위키 노트 — PNG 내용을 **표준 8섹션**으로 옮긴 결과물 |
| **인덱스 (연동)** | `0. Sub-Note/0. Keyword Note.md` → `### 보안 - *` | 분류별 키워드·암기법 표 |

```
NS반 강의 PDF
    ↓ (페이지별 캡처)
1. NS반/서브노트/보안/0007_암호학적 보안 강도.png  ← "캡처된 답안"
    ↓ (누락 평가)
0. Sub-Note/6. 보안/ 에 md 없음 → 작성 필요
    ↓ (PNG 내용 읽고 작성)
0. Sub-Note/6. 보안/암호학적 보안 강도.md  (#NEW)
```

**주의:** 주간모의고사 답안·Anki 카드는 이 워크플로우와 **별개**다. 본 가이드는 **NS반 서브노트 PNG 캡처만**을 기준으로 한다.

---

## 2. 전체 작업 흐름 (4단계)

```
[1] PNG 목록 추출          [2] 누락 평가              [3] PNG → 서브노트 작성       [4] 검증·인덱스
서브노트/보안/*.png    vs   6. 보안/*.md          PNG 내용 읽기            포맷·태그·Keyword Note
(98개 토픽명)              (매칭·동의어·제외)        8섹션 + #NEW              반영
```

| 단계 | 입력 | 출력 | 핵심 판단 |
|---|---|---|---|
| **1. 목록화** | PNG 파일명 | 토픽명 리스트 (번호 제거) | `0007_암호학적 보안 강도(...).png` → `암호학적 보안 강도` |
| **2. 평가** | PNG 리스트 + MD 리스트 | **누락 N개** / **기존 M개** / **제외 K개** | 동의어·약어 매칭으로 false positive 제거 |
| **3. 작성** | 누락 PNG 1장씩 | `.md` 서브노트 | PNG 슬라이드 내용 → 8섹션 변환, `#NEW` 태그 |
| **4. 검증** | 작성된 md | 통과/수정 목록 | 8섹션·태그·연관토픽·Keyword Note 행 추가 |

---

## 3. Phase 1 — PNG 토픽 목록 추출

### 3.1 파일명 규칙

```
{4자리번호}_{토픽명(영문 병기)}.png

예:
0001_암호화(Encryption).png
0007_암호학적 보안 강도(Security Strength).png
0056_DMARC(Domain-based Message Authentication...).png
```

### 3.2 토픽명 추출 코드

```python
import re, unicodedata

def extract_topic(png_filename):
    name = png_filename[:-4]                          # .png 제거
    name = re.sub(r'^\d+_', '', name)                 # 앞 번호 제거
    name = unicodedata.normalize('NFC', name)         # 한글 NFC (macOS NFD 대응)
    return name

# "0007_암호학적 보안 강도(Security Strength).png"
# → "암호학적 보안 강도(Security Strength)"
# md 파일명은 보통 괄호 앞 한글만: "암호학적 보안 강도.md"
```

### 3.3 보조 체크리스트 — `보안.md`

`1. NS반/서브노트/보안.md`는 캡처 내용을 **85개 항목**으로 나열한 목차다. PNG와 1:1 대응하며, 학습 메모(암기 포인트)가 붙어 있다.

```markdown
6. 암호학적 보안 강도
   1. 암호화 키 길이, 해시 출력 길이, 운용 모드, IV
   2. 쇼어, 그루버 알고리즘
15. 스니핑, 스푸핑
   1. 스니핑 도청, 스푸핑 속이기
72. [[SDP]]
   1. 제로트러스트와 SDP 그림이 같음
```

→ PNG 평가 후 **보안.md 항목**과도 대조하면, "캡처는 있는데 md가 없는" 누락을 이중 확인할 수 있다.

---

## 4. Phase 2 — 누락 평가 (PNG vs MD)

### 4.1 비교 대상

| | 경로 | 현재 수 |
|---|---|---|
| PNG (기준) | `1. NS반/서브노트/보안/` | 98 |
| MD (대상) | `0. Sub-Note/6. 보안/` | 138 |

MD가 PNG보다 많은 이유: PNG에 없지만 다른 출처(과년 기출·타 도메인)로 이미 작성된 노트가 있기 때문. **평가 방향은 PNG → MD (PNG 기준 누락만 찾기)**.

### 4.2 매칭 순서

각 PNG 토픽에 대해 아래 순서로 기존 MD와 매칭을 시도한다. **어느 단계에서든 매칭되면 "기존 있음"으로 분류**하고 작성하지 않는다.

| 순서 | 방법 | 설명 | 예시 |
|---|---|---|---|
| 1 | **동의어 사전** | 수동 매핑 exact | `Shannon의 암호 설계 원칙` → `샤논의 암호 설계 원칙` |
| 2 | **norm() exact** | 공백·특수문자 제거 후 일치 | `스니핑&스푸핑` ↔ `스니핑과 스푸핑` |
| 3 | **ko() 코어** | 괄호·영문 제거, 한글만 | `암호학적보안강도` ↔ `암호학적 보안 강도` |
| 4 | **leading_acr()** | 선두/괄호 내 영문 약어 | `DRDoS`, `CMMC`, `SDP`, `DMARC` |
| 5 | **ko() fuzzy** | 75% 이상 포함 (신중) | 긴 토픽명 부분 일치 |

```python
def norm(s):
    s = nfc(s).lower().replace('／','/').replace('：',':')
    s = re.sub(r'[\s_\-–—•·,，&]+', '', s)
    s = re.sub(r'[()（）\[\]【】]', '', s)
    return s

def ko(s):
    s = re.sub(r'[\(（][^)\）]*[\)）]', '', s)
    s = re.sub(r'[A-Za-z0-9./_\-：：／•·,&+]+', '', s)
    return re.sub(r'\s+', '', s)
```

### 4.3 보안 동의어 사전 (필수 — 중복 방지)

아래 PNG 토픽은 **이미 다른 이름의 md가 존재**하므로 새로 만들지 않는다.

| PNG 토픽 | 기존 MD (매칭됨) | 비고 |
|---|---|---|
| Shannon의 암호 설계 원칙 | `샤논의 암호 설계 원칙` | 한/영 표기 |
| Post-Quantum Cryptography | `양자내성암호화(PQC)` | |
| Diffie-Hellman Algorithm | `Diffie-Hellman 키 교환` | |
| APT 공격 | `APT` | |
| OWASP Top 10 for LLM 2025 | `OWASP LLM Top 10` | |
| DevSecOps | `Secure SDLC` 또는 SW공학 쪽 | 도메인 중복 주의 |
| 시큐어 코딩 | `SW개발보안(시큐어 코딩)` | |
| ISO 21434 | `ISO-21434` | 하이픈 유무 |
| 스마트팩토리 보안 | `스마트팩토리` (5. 디지털서비스) | 타 도메인 |
| 클라우드 컴퓨팅 취약점 | `클라우드컴퓨팅 취약점, 대응기술` | |
| PbD 인증제도 | `PbD(Privacy by Design)` | |
| 접근제어 / 접근통제 | `접근제어(AC)` | |
| TLS/SSL | `SSL_TLS` | |
| 디지털 포렌식 | `디지털 포렌식` | |
| 사이버 레질리언스 | `사이버 레질리언스` | |

### 4.4 흔한 오매칭 (주의)

| 오류 | 원인 | 올바른 처리 |
|---|---|---|
| ISO 27017/27018/27701/20889 → `ISO-21434`로 일괄 매칭 | substring match 과다 | **ISO 번호별 개별 비교** |
| `암호학적 보안 강도` → `CSAP` 매칭 | 약어/키워드 loose match | primary key + 동의어 사전 |
| `스마트시티 보안` → 기존 `스마트카` 매칭 | "스마트" 공통어 | 한글 코어 4자 이상 exact |

### 4.5 평가 결과 (2026-07-11 작업)

- PNG 98개 vs MD 112개(당시) → **누락 26개** 확정
- false positive 제거 전: ~30개 (매칭 로직 개선으로 16개로 축소 후, ISO·포렌식 등 재검토하여 26개 확정)

---

## 5. Phase 3 — PNG 내용 → 서브노트 작성

### 5.1 작성 원칙

1. **PNG 1장을 반드시 먼저 읽는다** — 슬라이드의 정의·키워드·표·도식을 그대로 반영
2. **기존 노트 포맷을 따른다** — `APT.md`, `암호학적 보안 강도.md` 등 기존 6. 보안 노트와 동일 구조
3. **신규 작성에는 `#NEW` 태그** — 첫 줄 태그에 포함
4. **파일명 = PNG 토픽명 (한글, NFC)** — 괄호 영문은 생략 가능

### 5.2 표준 8섹션 포맷

```markdown
#정보관리기술사 #보안 #MainTag #OtherTag #보안/분류경로 #NEW
## 정의
TopicName
- PNG 슬라이드의 핵심 정의 1~2 bullet
## 키워드
* kw1, kw2, kw3, kw4, kw5
## 암기법
* mnemonic: meaning
## 특징
- f1: brief
- f2: brief
- f3: brief
- f4: brief
## 목적
- p1
- p2
## 구성요소
- item: detail
## 구성도
```
(PNG 슬라이드 도식을 ASCII로 변환)
```
## 연관 토픽
- [[ExistingNote]] - brief
```

### 5.3 태그 규칙

| 태그 | 예시 | 필수 |
|---|---|---|
| `#정보관리기술사` | 공통 | ○ |
| `#보안` | 도메인 | ○ |
| 토픽 태그 | `#SDP`, `#DRDoS`, `#ISO27017` | ○ |
| 분류 경로 | `#보안/암호화`, `#보안/공격/위협` | ○ (Keyword Note 연동) |
| `#NEW` | PNG 기준 신규 | ○ (신규만) |

### 5.4 보안 6대 분류 (Keyword Note 섹션)

PNG 내용·토픽 성격에 따라 아래 중 하나를 `#보안/...` 경로로 지정한다.

| Keyword Note 섹션 | 해당 PNG 토픽 예 |
|---|---|
| **보안 - 암호화** | 암호화, 블록암호, 해시, 양자암호, 전자서명, 동형암호, 이중서명 |
| **보안 - 공격/위협** | APT, DRDoS, 스니핑·스푸핑, OWASP, SDP, SOC, DMARC |
| **보안 - 인증/접근제어** | OAuth, 패스키, VPN, TLS/SSL, 접근제어, ISMS-P |
| **보안 - 개발/포렌식** | 시큐어코딩, DevSecOps, 디지털 포렌식, 클라우드 포렌식, VEX |
| **보안 - 개인정보/정책** | PbD, 가명처리, PIA, 차분 프라이버시, MPC, 정보보호 공시 |
| **보안 - IoT/신기술** | IEC 62443, 스마트카, 블록체인, 드론, 자율주행·스마트시티·디지털트윈 보안 |

### 5.3 PNG → 섹션 매핑 예 (`암호학적 보안 강도`)

**PNG (0007):** 키 길이, 해시 출력, 운용 모드, IV/Nonce, 112/128/192/256bit, 쇼어·그루버

**작성 결과:**

```markdown
#정보관리기술사 #보안 #보안강도 #SecurityStrength #NEW
## 정의
암호학적 보안 강도(Security Strength)
- 특정 암호 알고리즘·해시 함수에 대해 공격자가 키 탐색·충돌 탐색·역상 탐색 등을
  성공시키는 데 필요한 계산량을 2^n 수준의 연산량으로 나타낸 척도
## 키워드
* 2^n, 연산량, 암호 키 길이, 해시 출력 길이, 운용 모드, IV·Nonce, 112/128/192/256bit
## 암기법
* 키해운IV: 키길이·해시출력·운용모드·IV/Nonce
...
## 연관 토픽
- [[블록 암호화]] - 운용 모드·키 길이
- [[양자내성암호화(PQC)]] - 양자 위협 대비 강도
```

---

## 6. 실제 보완 결과 — 추가 26개 (#NEW)

2026-07-11 NS반 PNG 98개 기준 평가 후 작성된 목록:

| 분류 | 추가 파일명 |
|---|---|
| **암호·공격** | `암호학적 보안 강도`, `스니핑과 스푸핑`, `BPF Door`, `부채널 공격`, `DRDoS`, `SW난독화` |
| **네트워크·콘텐츠** | `DNS 싱크홀`, `DRM`, `디지털 워터마킹`, `핑거프린팅`, `DMARC`, `SDP` |
| **표준·제도** | `ISO 27017`, `ISO 27018`, `ISO 27701`, `ISO-IEC 20889`, `전자증거개시제도`, `개인정보 영향평가`, `정보보호 공시제도`, `CMMC` |
| **IoT·신기술** | `사이버 게놈`, `클라우드 포렌식`, `블록체인 암호기술 가이드라인`, `자율주행 자동차 보안`, `스마트시티 보안`, `디지털 트윈 보안` |

### PNG 번호 ↔ 파일명 대응

| PNG # | PNG 토픽명 | md 파일명 |
|---|---|---|
| 0007 | 암호학적 보안 강도 | `암호학적 보안 강도.md` |
| 0016 | 스니핑 & 스푸핑 | `스니핑과 스푸핑.md` |
| 0017 | BPF Door | `BPF Door.md` |
| 0018 | 부채널 공격 | `부채널 공격.md` |
| 0022 | DRDoS | `DRDoS.md` |
| 0029 | SW난독화 | `SW난독화.md` |
| 0036 | DNS 싱크홀 | `DNS 싱크홀.md` |
| 0048 | DRM | `DRM.md` |
| 0049 | 디지털 워터마킹 | `디지털 워터마킹.md` |
| 0050 | 핑거프린팅 | `핑거프린팅.md` |
| 0056 | DMARC | `DMARC.md` |
| 0062 | ISO 27017 | `ISO 27017.md` |
| 0069 | ISO 27018 | `ISO 27018.md` |
| 0074 | ISO 27701 | `ISO 27701.md` |
| 0076 | ISO/IEC 20889 | `ISO-IEC 20889.md` |
| 0077 | 전자증거개시제도 | `전자증거개시제도.md` |
| 0078 | SDP | `SDP.md` |
| 0080 | 개인정보 영향평가(PIA) | `개인정보 영향평가.md` |
| 0084 | 정보보호 공시제도 | `정보보호 공시제도.md` |
| 0086 | CMMC | `CMMC.md` |
| 0087 | 사이버 게놈 | `사이버 게놈.md` |
| 0090 | 클라우드 포렌식 | `클라우드 포렌식.md` |
| 0092 | 블록체인 암호기술 가이드라인 | `블록체인 암호기술 가이드라인.md` |
| 0093 | 자율주행 자동차 보안 | `자율주행 자동차 보안.md` |
| 0094 | 스마트시티 보안 | `스마트시티 보안.md` |
| 0097 | 디지털 트윈 보안 | `디지털 트윈 보안.md` |

---

## 7. Phase 4 — 검증

### 7.1 파일 존재·태그 확인

```bash
# 26개 파일 존재 확인
ls "0. Sub-Note/6. 보안/" | grep -E '암호학적|스니핑|BPF|부채널|DRDoS|SW난독|DNS 싱크|DRM|워터|핑거|DMARC|ISO 270|20889|전자증거|SDP|영향평가|공시|CMMC|게놈|클라우드 포렌|블록체인|자율|스마트시티|트윈'

# #NEW 태그 확인
grep -l "#NEW" "0. Sub-Note/6. 보안"/*.md
```

### 7.2 8섹션·포맷 검증

각 md에 아래 섹션이 **모두** 있어야 한다:

```
## 정의  ## 키워드  ## 암기법  ## 특징  ## 목적  ## 구성요소  ## 구성도  ## 연관 토픽
```

첫 줄: `#정보관리기술사 #보안 ...` 로 시작 (공백·`.` 없이)

### 7.3 Keyword Note 반영

`0. Keyword Note.md`의 `### 보안 - {분류}` 표에 wiki link 행 추가:

```markdown
| 788 | 보안 | [[SDP]] | SDP(Software Defined Perimeter)... | 에이컨게: Agent·Controller·Gateway | |
```

- `## 정의`·`## 키워드`·`## 암기법`에서 추출
- `[[파일stem]]` = md 파일명(확장자 제외)과 **정확히 일치**

---

## 8. 재현 절차 (다른 사람용)

### 8.1 NS반 보안 PNG가 추가·갱신되었을 때

```
□ 1. 1. NS반/서브노트/보안/ PNG 전체 목록 추출 (§3.2)
□ 2. 0. Sub-Note/6. 보안/ md 전체 목록 추출
□ 3. §4.2 매칭 순서 + §4.3 동의어 사전으로 누락만 확정
□ 4. 1. NS반/서브노트/보안.md 목차와 교차 확인 (이중 검증)
□ 5. 누락 PNG를 1장씩 열어 내용 확인
□ 6. §5.2 포맷 + #NEW + 분류 태그로 md 작성
□ 7. §7 검증 (8섹션, 태그, wiki link)
□ 8. Keyword Note 해당 분류 표에 행 추가
□ 9. (선택) #NEW 검수 후 제거
```

### 8.2 PNG 캡처 방법 (참고)

- NS반 PDF는 DRM 보호(Drmviewer) → **자동 캡처 불가**, 수동 캡처 필요
- 기존 PNG 해상도: 2560×1440 (보안 폴더 기준)
- 파일명: `{번호4자리}_{토픽명}.png` 형식 유지
- 저장 위치: `1. NS반/서브노트/보안/`

---

## 9. 현재 현황

| 항목 | 수치 |
|---|---|
| 캡처 PNG (`서브노트/보안`) | 98 |
| 서브노트 md (`6. 보안`) | 138 |
| PNG 기준 #NEW 추가 | 26 (+ 스텁 보완 4) |
| Keyword Note 보안 분류 | 6개 섹션 |

---

## 10. 관련 파일

```
1. ITPE/
├── 0. Sub-Note/
│   ├── 0. Keyword Note.md              ← 보안 6분류 인덱스
│   ├── 6. 보안/                        ← 서브노트 (138개)
│   └── 답안분석 및 서브노트 보완 맥락.md  ← 본 문서
└── 1. NS반/
    └── 서브노트/
        ├── 보안.md                     ← 캡처 토픽 목차 (85항목)
        └── 보안/                       ← 캡처 PNG (98개) ★ 기준
```

---

*본 가이드는 NS반 서브노트 PNG 98개 vs `6. 보안` md 비교 → 누락 26개 `#NEW` 작성 작업을 재현 가능하도록 정리한 문서이다.*
