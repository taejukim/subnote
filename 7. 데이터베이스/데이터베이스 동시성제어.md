#정보관리기술사 #데이터베이스 #데이터베이스동시성제어 #데이터베이스/트랜잭션/회복
## 정의
- 다중 사용자 환경을 지원하는 데이터베이스 시스템에서 여러 트랜잭션들이 직렬성 및 무결성을 보장하고 성공적으로 동시에 실행될 수 있도록 지원하는 제어 기법
## 키워드
- 직렬성, 무결성, Locking, 2PL, Timestamp, 낙관적 검증, MVCC

## 동시성제어를 하지 않을 경우 문제점 [갱현모연]

| 문제점 | 설명 |
|------|------|
| **갱**신손실 (Lost Update) | – 하나의 트랜잭션이 수행한 데이터 변경 연산의 결과를 다른 트랜잭션이 덮어서 변경 연산이 무효화되는 것<br>– 트랜잭션이 동일 데이터를 동시에 갱신할 경우 발생 |
| **현**황파악오류 (Dirty Read) | – 트랜잭션 중간 수행결과를 다른 트랜잭션이 참조하면서 발생 |
| **모**순성 (Inconsistency) | – 하나의 트랜잭션이 여러 개의 데이터 변경 연산을 실행할 때 일관성 없는 상태의 데이터베이스에서 데이터를 가져와 연산을 실행하여 모순된 결과가 발생하는 것<br>– 상호간섭에 의해 일관성이 없는 상태로 변질된 오류 |
| **연**쇄복귀 (Cascading Rollback) | – 트랜잭션이 완료되기 전에 장애가 발생하여 rollback을 수행하면 이 트랜잭션이 장애 발생 전에 변경한 데이터를 가져가 변경 연산을 실행한 또 다른 트랜잭션에도 rollback 연산을 연쇄적으로 수행해야 함 |

---

## ■ 동시성 제어기법 유형 [회로(즉지)체그아] + [2PL낙타다]

| 기법                    | 개념도                                           | 설명                                                                                                              |
| --------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Locking               | ![[1. ITPE/0. Sub-Note/99.attached_file/Pasted image 20260331235255.png\|300]]<br> | – 하나의 트랜잭션이 실행하는 동안 다른 트랜잭션이 동시에 접근하지 못하도록 상호 배제<br>– Lock연산과 Unlock 연산 사용                                      |
| 2PL (Phase Locking)   | ![[1. ITPE/0. Sub-Note/99.attached_file/Pasted image 20260331235306.png\|300]]     | – 모든 트랜잭션이 Lock/Unlock 연산을 확장 및 수축단계로 구분되어 직렬성 보장<br>– 확장단계에서는 Lock, 수축단계에서는 Unlock 연산만을 수행                     |
| 낙관적검증 기법 (Validation) | ![[1. ITPE/0. Sub-Note/99.attached_file/Pasted image 20260331235316.png\|300]]     | – 트랜잭션 수행시간은 어떠한 직렬성보장을 위한 작업을 수행하지 않고, 종료 시 일괄적으로 검증하여 일관성을 보장<br>– Read, Validation, Execution의 3단계 수행        |
| Timestamp ordering    | ![[1. ITPE/0. Sub-Note/99.attached_file/Pasted image 20260331235329.png\|300]]     | – DB에서 부여되는 식별자인 Timestamp를 이용 시간대별로 직렬화 시켜 동시성 제어<br>– 충돌되는 Read나 Write 연산을 타임스탬프 순서대로 실행                      |
| 다중버전 동시성제어 (MVCC)     | ![[1. ITPE/0. Sub-Note/99.attached_file/Pasted image 20260331235346.png\|300]]     | – 트랜잭션이 한 데이터에 접근할 때 해당 트랜잭션의 타임스탬프와 여러 접근하려는 데이터의 여러 버전의 타임스탬프를 비교하여, 현재 실행하고 있는 스케줄의 직렬 가능성이 보장되는 버전을 선택하여 접근 |
