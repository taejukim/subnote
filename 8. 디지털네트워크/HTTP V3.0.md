#디지털네트워크 #HTTP #QUIC #옵션 #디지털네트워크/OSI/프로토콜
## 정의
QUIC 기반 차세대 웹 프로토콜, HTTP/3
- UDP 기반 QUIC 프로토콜 위에서 동작하는 차세대 HTTP 표준
- Google이 개발한 QUIC을 IETF가 표준화하여 2022년 RFC 9114로 발표
## 키워드
* QUIC, UDP, 0-RTT, 멀티플렉싱, HOL Blocking 해결, TLS 1.3 내장
## 암기법
* 큐제멀영: QUIC기반, 0-RTT(제로RTT), 멀티플렉싱, 영속연결
* HTTP버전: 1.0(단순)→1.1(지속)→2.0(멀티)→3.0(QUIC)
## HTTP 버전별 비교
| 구분 | HTTP/1.1 | HTTP/2.0 | HTTP/3.0 |
|------|----------|----------|----------|
| 전송계층 | TCP | TCP | UDP(QUIC) |
| 연결방식 | Keep-Alive | 멀티플렉싱 | 멀티플렉싱 |
| HOL Blocking | 발생 | TCP레벨 발생 | 해결 |
| 핸드쉐이크 | 3-Way | 3-Way | 0-RTT/1-RTT |
| 암호화 | 선택(TLS) | 선택(TLS) | 필수(TLS 1.3) |
| 헤더압축 | 없음 | HPACK | QPACK |
## 특징
| 구분 | 설명 | 비고 |
|------|------|------|
| QUIC 기반 | UDP 위에 신뢰성 구현 | TCP 대체 |
| 0-RTT | 재연결 시 즉시 데이터 전송 | 지연시간 최소화 |
| 독립 스트림 | 스트림별 독립 전송 | HOL Blocking 해결 |
| 연결 마이그레이션 | IP 변경 시 연결 유지 | Connection ID 활용 |
| 내장 암호화 | TLS 1.3 기본 탑재 | 보안 강화 |
## 구성도
```
[HTTP 버전별 비교]

HTTP/1.1          HTTP/2.0           HTTP/3.0
┌────────┐       ┌────────┐        ┌────────┐
│ HTTP   │       │ HTTP/2 │        │ HTTP/3 │
├────────┤       ├────────┤        ├────────┤
│  TLS   │       │  TLS   │        │  QUIC  │←TLS 1.3 내장
├────────┤       ├────────┤        ├────────┤
│  TCP   │       │  TCP   │        │  UDP   │
├────────┤       ├────────┤        ├────────┤
│   IP   │       │   IP   │        │   IP   │
└────────┘       └────────┘        └────────┘

[HOL Blocking 해결]
HTTP/2 (TCP)                HTTP/3 (QUIC)
Stream1 ─┬─ TCP ─┬─        Stream1 ───────────▶
Stream2 ─┤      ├─ 손실    Stream2 ───────────▶
Stream3 ─┘      └─ 전체대기 Stream3 ──X(손실)──▶
                           (다른 스트림 영향 없음)
```
![[Pasted image 20260325230558.png|500]]
## HOL(Head Of Line) 블로킹
- 패킷을 대기 행렬에 큐잉하여 FIFO 처리함 (대기열의 머리에 있는 패킷은 대기열의 끝에 있는 패킷보다 먼저 전달
- 순차 처리 제약으로 인해 머리가 처리되지 않으면 후속 패킷은 대기하게됨
- 동일한 송신포트에 대한 처리량 경쟁으로 처리량 지연 및 프레임 손실 발생
## 동작 과정
| **구분**                         | **동작 과정**                        | **설명**                                                       |
| ------------------------------ | -------------------------------- | ------------------------------------------------------------ |
| **Initial 1-RTT Handshake**    | **Inchoate CHLO** (Client Hello) | - 시작을 알리는 Inchoate(시작 단계)<br>- 암호화되지 않은 CHLO 패킷을 전송          |
|                                | **Rejection**                    | - 서버 설정과 암호화된 토큰을 포함한 패킷 전송                                  |
|                                | **Complete CHLO**                | - 연결 완료, 이후부터 암호화된 통신 가능                                     |
| **Successful 0-RTT Handshake** | **Complete CHLO**                | - 이전 연결 시 캐싱된 자격증명을 사용해서 **Encrypted Request**를 서버로 바로 전송 가능 |
| **Rejected 0-RTT Handshake**   | **1-RTT Handshake 재 수행**         | - 캐싱된 정보가 오래된 경우에 수행하는 동작, 이때에는 1-RTT 재 수행                   |
## 연관 토픽
- [[TCP]] - 전송 제어 프로토콜
- [[UDP]] - 비연결형 전송 프로토콜
- [[SSL_TLS]] - 보안 프로토콜
